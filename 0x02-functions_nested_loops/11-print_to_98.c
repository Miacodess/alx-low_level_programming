#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - prints to 98
 * @m: number to start from
 * Return: 0 or 1
 */

void print_to_98(int m)
{
	while (m < 98)
	{
		printf("%i, "' n);
		n++;
	}
	while (n > 98)
	{
		printf("%i, ", n);
		n--;
	}
	printf("98");
	putchar('\n');
}
