#include "main.h"
#include <stdio.h>

/**
 * cap_string - capitalizes all words of a string
 * @s : string
 *
 * Return: 0 (Success)
 */

char *cap_string(char *s)
{
	int i = 0;

	while (s[i])
	{
	}
}
